export type ScheduledNotificationActionState = {
  ok: boolean;
  message: string;
};

export const initialScheduledNotificationActionState: ScheduledNotificationActionState = {
  ok: false,
  message: "",
};
